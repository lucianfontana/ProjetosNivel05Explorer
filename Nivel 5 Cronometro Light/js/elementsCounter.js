const playOn = document.querySelector(".playOn")
const playOff = document.querySelector(".playOff")
const stopOn = document.querySelector(".stopOn")
const stopOff = document.querySelector(".stopOff")
const pause = document.querySelector(".pause")
const addMin = document.querySelector(".addMin")
const addMinMax = document.querySelector(".addMinMax")
const decMin = document.querySelector(".decMin")
const DisplayMinutes = document.querySelector(".minutes")
const DisplaySeconds = document.querySelector(".seconds")


export{
  playOn,
  playOff,
  stopOn,
  stopOff,
  pause,
  addMin,
  addMinMax,
  decMin,
  DisplayMinutes,
  DisplaySeconds
}